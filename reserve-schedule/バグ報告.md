# 予約システム バグ報告（独立レビュー）

対象: `reserve-schedule/index.html` / `admin.html` / `shared.js`
ブランチ: `claude/salon-booking-system-qoszvb`
レビュー日: 2026-08-13
レビュー方針: 実装者とは独立の視点で粗探しを行い、**推測ではなく実際に動かして再現確認する**ことを原則とした。

## 検証環境

- Chromium（Playwright、`/opt/pw-browsers/chromium-1194`）+ `python3 -m http.server` によるローカル配信
- Node 22 上で `shared.js` を直接読み込むユニットテスト（`window` / `localStorage` をスタブ化）
- 時刻依存の検証は Playwright の Clock API（`page.clock.install` / `fastForward`）で時計を固定・早送りして再現

各項目には以下を明記した。

- **【再現済み】** … 実際に動かして症状を確認したもの
- **【コード読解のみ】** … コードを読んで問題と判断したが、実挙動の再現はしていないもの

前提として指示された既知事項（localStorage 保存、通知未実装、`OPTION_ITEMS` 仮データ、`TIME_WINDOW_MODE="end"`、予約番号が長い内部ID）は報告に含めていない。

---

# 深刻度: 致命的

## [致命的] 同時に予約が入ると、両方のお客様に「予約完了」が表示されたうえで片方の予約が消滅する 【再現済み】

- **場所**: `shared.js:438-468`（`Ledger.api.createReservation`）。重複チェック `443-451` と保存 `462-463` の間、および `loadReservations()`（`198`）→`saveReservations()`（`204`）という read-modify-write 全体。
- **再現手順**:
  1. 同一ブラウザで予約ページを2つのタブで開く。
  2. 両タブでまったく同じ日時（例: 5日後 14:00〜15:30）に対して `createReservation` を**同時に**発火させる。
     実測は Playwright で `Promise.all([tabA.evaluate(create), tabB.evaluate(create)])` として10回試行。
  3. 各回、両タブの戻り値 `ok` と、`localStorage` に最終的に残った予約件数を確認。
- **何が起きるか**: **10回中9回**、両タブとも `ok:true`（＝両方のお客様の画面に「ご予約ありがとうございます」の完了画面が出る）となった。しかもその9回すべてで、`localStorage` に残った予約は**1件だけ**だった。つまり、
  - 一方のお客様は完了画面を見たのに、予約がどこにも存在しない（無断キャンセル扱いになる）。
  - もし保存が両方成功する経路（フェーズ②のサーバー化後の素朴な実装）になれば、今度は本物のダブルブッキングになる。
  - なお**逐次**（タブ2が確定 → その後タブ1が確定）の場合は正しく拒否された。壊れるのは並行時のみ。
- **あるべき挙動**: 重複チェックと保存が不可分（アトミック）に行われ、後から来た側は必ず「この時間帯はすでに予約されています」で拒否される。予約が消えることは絶対にあってはならない。
- **修正案**:
  - **フェーズ②（本命）**: D1 側で `reservations(date_iso, start_min)` に一意制約を張るか、`INSERT ... SELECT ... WHERE NOT EXISTS (重なる行)` を1本のSQLで実行し、影響行数0なら競合として拒否する。「SELECT で確認してから INSERT」という2文構成にしないこと。
  - **フェーズ①の暫定策**: `createReservation` の中で `loadReservations()` → 重複チェック → `saveReservations()` を実行したあと、**保存直後にもう一度読み直して自分のIDが残っているか・重なる他予約が混入していないかを検証**し、崩れていればロールバックしてエラーを返す。または `navigator.locks.request()` で同一オリジン内の排他を取る。
  - ※「localStorage であること」自体は既知の前提だが、**この競合はフェーズ②の設計にそのまま持ち込むと本物のダブルブッキングになる**ため、致命的として報告する。

---

# 深刻度: 重大

## [重大] 画面を開いたまま時間が経つと、受付締切（2時間前）を過ぎた枠・過去の日時をそのまま予約できてしまう 【再現済み】

- **場所**: `shared.js:438-468`（`createReservation` が `computeAvailableStartTimes` / `isBookableDate` / `CUTOFF_MIN` を一切再検証していない）。空き判定は `shared.js:369-413` にあるが、確定時には呼ばれない。
- **再現手順（受付締切）**:
  1. 時計を 2026-08-13 13:00 に固定して予約ページを開く。
  2. 60分メニューを選び、STEP2 で「本日」を選択。空き枠は `15:30, 16:00, …`（＝2時間後以降）と正しく表示される。
  3. 15:30 を選び、STEP3 でお客様情報を入力して STEP4 まで進む。
  4. **STEP4 に留まったまま時計を 14:30 に進める**（15:30 開始は残り1時間＝締切超過）。
  5. 規約に同意して「予約を確定する」を押す。
- **何が起きるか**: **そのまま確定し、完了画面が表示される。** 保存された予約は `2026-08-13 / startMin=930（15:30）`、確定操作の時点で現在時刻は 14:30。締切2時間前ルールを破った予約がサロン側に入る。
- **再現手順（日付変更を跨ぐ）**:
  1. 時計を 2026-08-13 20:00 に固定してページを開く。本日の残り枠 `22:30, 23:00` が表示される。
  2. 22:30 を選んで STEP4 まで進む。
  3. **時計を6時間進める（＝8/14 02:00）**。
  4. 「予約を確定する」を押す。
- **何が起きるか**: **確定できてしまう。** 保存されるのは `2026-08-13 22:30`、つまり**すでに過ぎ去った日時の予約**。管理画面の予約一覧にも「過去の予約」として残り続ける。
- **あるべき挙動**: 確定処理の中で、確定時点の現在時刻を使って「予約可能範囲内か」「受付締切を過ぎていないか」を再判定し、外れていれば「申し訳ございません。受付を締め切りました。別の時間をお選びください」と表示して STEP2 に戻す。
- **修正案**: `createReservation` の冒頭で、重複チェックと同じ場所に以下を追加する。
  ```js
  var cfg = getEffectiveConfig();
  var ok = computeAvailableStartTimes({
    dateISO: payload.dateISO, totalMinutes: payload.totalMinutes, now: new Date(),
    reservations: loadReservations().filter(r=>r.dateISO===payload.dateISO),
    blockedSlots: loadBlockedSlots().filter(b=>b.dateISO===payload.dateISO),
    serviceWindow: /* menuIds から再計算 */, cfg: cfg
  }).indexOf(payload.startMin) !== -1;
  if (!ok) return { ok:false, error:"..." };
  ```
  既存の純粋関数をそのまま使えるので追加ロジックは不要。**この1箇所を直せば、次の項目の大半も同時に塞がる。**

## [重大] `createReservation` が payload をまったく検証せず、不正な予約データを受け入れる 【再現済み】

- **場所**: `shared.js:438-468`
- **再現手順**: Node 上で `shared.js` を読み込み、`Ledger.api.createReservation()` に以下の payload を直接渡した（フェーズ②で同じシグネチャの HTTP API になる前提での検証）。
- **何が起きるか**: すべて `ok:true` で保存された。

  | 渡した内容 | 結果 |
  | --- | --- |
  | `dateISO` が30日前 | ★作成できる |
  | `dateISO` が400日先（`MAX_ADVANCE_DAYS` 超過） | ★作成できる |
  | `startMin:180, endMin:240`（03:00〜04:00・営業時間外） | ★作成できる |
  | `startMin:720, endMin:727`（7分・`STEP=30` 違反） | ★作成できる |
  | `startMin:840, endMin:780`（終了が開始より前） | ★作成できる |
  | `totalPrice:-99999, totalMinutes:-60` | ★作成できる |
  | `customer:{}`（氏名・電話・メールすべてなし） | ★作成できる |
  | あかすりの `serviceWindow`（12:00〜20:00）を無視した 12:00〜23:00 | ★作成できる |
  | `dateISO:"not-a-date"` | ★作成できる |
  | 氏名20万文字 | ★作成できる |

- **あるべき挙動**: 保存前に、日付形式・予約可能範囲・受付締切・営業時間内・`STEP` の倍数・`endMin > startMin`・`endMin - startMin === totalMinutes`・メニューIDの実在・`totalMinutes`/`totalPrice` のサーバー側再計算・必須顧客項目の存在と形式・各文字列の最大長を検証すること。
- **修正案**: `createReservation` の先頭に `validateReservationPayload(payload)` を新設し、`Ledger.api` の外側（純粋関数群）に置く。前項の可用性再検証と合わせて実装すれば1箇所で済む。**フェーズ②で `Ledger.api` を fetch に差し替えると、この payload はネットワーク越しに誰でも投げられる値になるため、サーバー側実装時には必須。**
- **補足**: 画面（`index.html`）側のバリデーションは正しく機能している（氏名・フリガナ必須、`TEL_RE=/^0\d{9,10}$/`、`EMAIL_RE`）。問題はデータ層に検証が一切ないこと。

## [重大] localStorage の JSON が壊れると、全予約が黙って消え、次の保存で完全に上書きされる 【再現済み】

- **場所**: `shared.js:198-203`（`loadReservations` の `catch(e){ return []; }`）。`loadBlockedSlots`（`210-215`）・`loadSettings`（`169-174`）も同じ構造。
- **再現手順**:
  1. 予約を数件作成する。
  2. `localStorage.setItem("cocosiaBooking.reservations", "{壊れたJSON")` で保存値を破損させる（拡張機能の干渉、容量超過による書き込み途中終了、別タブとの競合などで起こりうる）。
  3. `Ledger.api.getReservations({})` を呼ぶ → **件数0**。エラーも警告も出ない。
  4. その状態で新しい予約を1件作成する。
  5. 再度 `getReservations({})` を呼ぶ → **件数1**（新しい予約のみ）。
- **何が起きるか**: 管理画面には「確定した予約はまだありません。」と表示される。オーナーは全予約が消えたことに気づかないまま営業し、次に1件でも予約が入った瞬間に**壊れた元データが正常なJSONで上書きされ、復旧手段が完全に失われる**。
- **あるべき挙動**: パース失敗は「データが空」ではなく「読み取り不能」として区別し、上書き保存を止めたうえで画面に明示的なエラーを出す。
- **修正案**: `loadReservations` を `{ ok:true, list }` / `{ ok:false, raw }` を返す形にし、`ok:false` のときは `saveReservations` を実行させない。最低限の暫定策として、`catch` の中で破損した生データを `cocosiaBooking.reservations.corrupt.<timestamp>` に退避したうえで `console.error` を出し、画面にも警告を表示する。

## [重大] 管理画面「設定」の予約可能日数が、お客様のカレンダーに反映されない 【再現済み】

- **場所**: `index.html:754`（`var CFG = L.BOOKING_CONFIG;`）と、それを使う `index.html:944`（`L.bookableRange(now, CFG)`）・`index.html:966`（`L.isBookableDate(dISO, now, CFG)`）。
  一方 `shared.js:548-562`（`getAvailability`）は `getEffectiveConfig()` を使っている。**同じルールを2箇所が別々の設定値で判定している。**
- **再現手順（範囲を狭めた場合）**:
  1. 管理画面「設定」で「予約可能日数」を **7** にして保存（`localStorage` の `settings.maxAdvanceDays = 7` を確認）。
  2. 予約ページを開き、メニューを選んで STEP2 へ。
  3. カレンダーを見る。
- **何が起きるか**: **8月13日〜31日の19日ぶんが選択可能なまま表示される**（本来は7日先までの8日ぶんだけのはず）。8日以降の日をクリックすると、灰色で無効化されるのではなく「**この日は、ご希望の合計時間で確保できる空き枠がありません。他の日をお選びください。**」と表示される。お客様には「その日は満席」に見えるが、実際は受付範囲外なだけで、まったく別の意味のメッセージが出ている。
- **再現手順（範囲を広げた場合）**:
  1. 「予約可能日数」を **120** にして保存。
  2. 予約ページ STEP2 でカレンダーの「›」を押し続ける。
- **何が起きるか**: **2026年10月で「›」が無効化され、それ以上先へ進めない**（＝60日で頭打ち）。オーナーが受付範囲を広げても、お客様は61日目以降を予約できない。
- **あるべき挙動**: カレンダーの選択可否も、管理画面で保存された `maxAdvanceDays` を使って判定する。
- **修正案**: `index.html` の初期化で `L.api.getSettings()` を呼び、その結果を `CFG` に代入してから `renderCalendar()` を呼ぶ。`renderCalendar` 内の `bookableRange` / `isBookableDate` にも同じ実効設定を渡す。あわせて「範囲外の日」と「満席の日」でメッセージを出し分ける。
- **参考**: 営業時間の設定（12:00→10:00〜18:00）は `getAvailability` 経由なので**正しく反映された**（枠が `10:00..17:00` の15件になることを確認）。ずれているのは日付範囲の判定だけ。

## [重大] 営業開始 > 営業終了 の設定を検証なしで保存でき、予約システム全体が停止する 【再現済み】

- **場所**: `admin.html:754-777`（設定フォームの submit ハンドラ。`businessStart` と `businessEnd` の前後関係を検証していない）。`shared.js:575-585`（`updateSettings` も検証なし）。
- **再現手順**:
  1. 管理画面「設定」で営業開始 `22:00`、営業終了 `13:00` にして「設定を保存する」を押す。
- **何が起きるか**: 「**設定を保存しました**」と表示され、`{businessStart:1320, businessEnd:780}` がそのまま保存される。その結果、
  - 管理画面の週間カレンダーが**行数0**になり、真っ白になる。
  - お客様側は**どの日を選んでも「空き枠がありません」**になる（`computeAvailableStartTimes` の `for` ループが `start + total <= end` を最初から満たさず、1件も返さない）。
  - 警告もエラーも一切出ないため、オーナーは「なぜか誰も予約できない」状態の原因にたどり着けない。
- **あるべき挙動**: 保存前に `businessStart < businessEnd` を検証し、満たさなければ保存を拒否してエラーを表示する。
- **修正案**: `admin.html:754` の submit ハンドラで、`payload.businessStart >= payload.businessEnd` なら `showToast("営業終了時刻は営業開始時刻より後にしてください")` して `return`。同じ検証を `shared.js` の `updateSettings` にも入れる（ブロック時間の登録では `admin.html:668` で同様の検証が既に行われているので、それに揃える）。

---

# 深刻度: 軽微

## [軽微] STEP4「ご予約内容の確認」がスマホ幅375pxで横にはみ出す 【再現済み】

- **場所**: `index.html:347-349`（`.confirm-row .k{ flex:0 0 auto; }` / `.confirm-row .v` に折り返し指定がない）
- **再現手順**: 375×812 で予約ページを開き、メニューを選んで STEP4 まで進み、`document.documentElement.scrollWidth` を測る。
- **何が起きるか**: メニュー名が長いものでページ全体が横スクロールする。

  | 選択メニュー | scrollWidth（clientWidth=375） |
  | --- | --- |
  | 【ストレッチ＋もみほぐし】全身すっきりコース | **432px**（`.k` が幅347pxのまま縮まず、価格 `.v` が画面外へ） |
  | 【痩身・フェイシャル】全身マシンコース | **406px** |
  | 【本格火罐カッピング】＋選べるオプション | **405px** |
  | 【毛穴洗浄×美白】エクソソーム導入（短い） | 375px（正常） |

  STEP1/2/3/5、予約照会パネル、管理画面の calendar/blocked/settings タブは375pxで問題なし。
- **あるべき挙動**: 予約直前の最重要画面で価格が画面外に出ないこと。
- **修正案**: `.confirm-row .k{ flex:1 1 auto; min-width:0; }`、`.confirm-row .v{ flex:0 0 auto; white-space:nowrap; }` に変更する（メニュー名側を折り返し、金額側を固定する）。

## [軽微] ご要望欄に長いURL（改行のない英数字）を入れると、確認画面と管理画面が大きく横に伸びる 【再現済み】

- **場所**: `index.html:349`（`.confirm-row .v`）、`admin.html:181`（`.res-card .note`）。いずれも `word-break` / `overflow-wrap` の指定がない。
- **再現手順**:
  1. 375px幅で、ご要望欄に `https://example.com/aaaa…`（英数字300文字）を入力して STEP4 へ。
  2. 同じ内容の予約を管理画面「予約一覧」で表示する。
- **何が起きるか**: STEP4 の `scrollWidth` が **2272px**、管理画面の予約一覧が **1010px** になり、レイアウトが大きく崩れて横スクロールが発生する。日本語の長文（300文字）や長い氏名では発生せず、**改行位置のない英数字列でのみ**発生する。長いメールアドレスでも `.grid2` 内で軽微な溢れを確認した。
- **あるべき挙動**: 長い文字列は折り返して表示される。
- **修正案**: `.confirm-row .v` と `.res-card .note`、`.res-card .grid2 div` に `overflow-wrap:anywhere;`（または `word-break:break-all;`）と `min-width:0;` を追加する。

## [軽微] 管理画面のブロック終了時刻に「24:00」がなく、23:30〜24:00 の枠だけ時間帯ブロックできない 【再現済み】

- **場所**: `admin.html:644-652`（`fillTimeOptions`。`for (var m = 0; m < 24*60; m += 30)` のため最大が 23:30）
- **再現手順**: 管理画面「休業日・ブロック」タブで「終日休業にする」のチェックを外し、終了時刻の選択肢を確認する。
- **何が起きるか**: 選択肢は `00:00 … 23:30` の48件で、**`24:00` が存在しない**。営業時間は 24:00 までなので `23:30〜24:00` の1枠が生まれるが、この枠だけ時間帯指定ではブロックできない（`admin.html:668` の `startMin >= endMin` チェックにより 23:30→23:30 も不可）。終日休業にするしか手がない。
- **あるべき挙動**: 終了時刻の選択肢に営業終了（24:00）まで含める。
- **修正案**: `fillTimeOptions` に「開始用/終了用」の区別を持たせ、終了用は `m <= 24*60` までループする。または `fillTimeOptions(select, {includeEnd:true})` とする。

## [軽微] 「⟳ 最新の状態に更新」や設定保存を行うと、ブロック時刻の選択が 00:00 にリセットされる 【再現済み】

- **場所**: `admin.html:786`（refresh ハンドラ内の `fillTimeOptions(blockStart); fillTimeOptions(blockEnd);`）、`admin.html:770`（設定保存後も同様）。初期化時（`admin.html:801-802`）だけは直後に `blockStart.value` を再設定しているが、この2箇所では再設定していない。
- **再現手順**:
  1. 管理画面「休業日・ブロック」で「終日休業にする」のチェックを外す（初期値 12:00 / 13:00）。
  2. 開始 15:00、終了 17:00 に変更する。
  3. ヘッダーの「⟳ 最新の状態に更新」を押す。
- **何が起きるか**: `fillTimeOptions` が `innerHTML` を作り直すため、**開始・終了とも 00:00 に戻る**（値の再設定がない）。入力途中の内容が失われる。気づかずに日付だけ入れて「登録する」を押すと「終了時刻は開始時刻より後にしてください」というエラーになり、原因がわかりにくい。設定保存後も同じ状態になる。
- **あるべき挙動**: 更新操作でブロック登録フォームの入力内容が失われないこと。
- **修正案**: そもそも `fillTimeOptions` は営業時間設定に依存しない固定リストなので、refresh 時に呼び直す必要がない。呼び直すのであれば、初期化時と同様に直後に元の値を復元する。

## [軽微] 営業時間外に登録した時間帯ブロックが、管理画面の週間カレンダーに一切表示されない 【再現済み】

- **場所**: `admin.html:490`（`for (var min = start; min < end; min += step)` が営業時間内の行しか描画しない）。一方 `admin.html:646` の選択肢は 00:00 から選べる。
- **再現手順**:
  1. 「休業日・ブロック」で、本日の `09:00〜11:00`（営業開始12:00より前）を理由「朝の用事」で登録する。
  2. 「週間カレンダー」タブを見る。
- **何が起きるか**: **`.cal-slot.busy` が0個**。カレンダー上にはブロックが一切現れない。ブロック一覧タブには正しく表示されるため、登録できたのに見えないという食い違いが起きる。
- **あるべき挙動**: 営業時間外の時間を選べないようにするか、選べるならカレンダーにも表示する。
- **修正案**: `fillTimeOptions` の選択肢を営業時間内（`BUSINESS_START`〜`BUSINESS_END`）に限定するのが簡単。営業時間外のブロックを許すなら、週間カレンダーの描画範囲を「営業時間 ∪ ブロックの時間帯」に広げる。

## [軽微] メニューを変更して STEP2 に戻ると、古いメニューの空き枠一覧と日付見出しが残ったままになる 【再現済み】

- **場所**: `index.html:824-834`（`showStep`。STEP2 に入るとき `renderCalendar()` は呼ぶが `timeSlotSection` を初期化しない）。日時のクリアは `index.html:866` / `890` で行われている。
- **再現手順**:
  1. 60分メニュー（エクソソーム導入）を選び、STEP2 で日付と時刻を選ぶ（枠は23件表示される）。
  2. 「戻る」で STEP1 に戻り、60分メニューのチェックを外して 120分メニュー（全身マシンコース）を選ぶ。
  3. 「次へ」で STEP2 に進む。
- **何が起きるか**: **60分メニュー時に取得した23件の枠（12:00〜23:00）と、古い日付の見出しがそのまま残って表示される。** 120分メニューでは本来 12:00〜22:00 の21件のはずで、末尾の 22:30・23:00 は選べない。1つのボタンが「選択済み」の見た目のままでもある。
  なお内部状態（`state.dateISO`）はクリアされているため、残留ボタンを押しても「次へ」は無効のままで、**不整合な予約は作られない**。実害はUIの混乱にとどまる。
- **あるべき挙動**: メニュー変更で日時がリセットされたら、時間帯セクションも非表示に戻す。
- **修正案**: `showStep` の `if (n === 2)` の分岐で、`state.dateISO` が `null` のときは `timeSlotSection.style.display = "none"; timeGrid.innerHTML = "";` を実行する。

## [軽微] 予約照会で、ハイフン付きの電話番号を入れるとヒットしない 【再現済み】

- **場所**: `index.html:1251` / `index.html:1254`（`r.customer.tel === tel` の完全一致比較。メールは `L.normalizeEmail` で正規化しているのに、電話番号には正規化がない）
- **再現手順**:
  1. 電話番号 `09099998888` で予約を作成する。
  2. 「予約の確認・変更」で、お電話番号に `090-9999-8888`、メールアドレスに登録済みのものを入力して「確認する」を押す。
- **何が起きるか**: **0件**。「条件に一致する、本日以降のご予約が見つかりませんでした」と表示される。照会フォームの電話番号欄には形式の検証も注記もないため、お客様は普段どおりハイフン付きで入力しがちで、予約が存在するのに見つからない。
- **あるべき挙動**: ハイフンや全角数字の有無にかかわらず照会できる。
- **修正案**: `normalizeEmail` と同様に `normalizeTel(s)`（`String(s).replace(/[^0-9]/g,"")` ＋ 全角→半角変換）を `shared.js` に追加し、照会時の比較と保存時の両方に適用する。
- **補足**: メールアドレスの大文字小文字ゆれ（`Look@Example.COM` で登録 → `look@example.com` で照会）は**正しくヒットした**。

## [軽微] 管理画面の週表示が、年をまたぐ週で開始年しか表示しない 【再現済み】

- **場所**: `admin.html:473`（`... + "  (" + days[0].getFullYear() + ")"`）
- **再現手順**: 管理画面の週間カレンダーで「›」を押して 2026年12月27日の週まで進める。
- **何が起きるか**: 「**12/27 〜 1/2  (2026)**」と表示される。1/1・1/2 は 2027年だが、表示上は2026年の週に見える。次の週は「1/3 〜 1/9  (2027)」。
- **あるべき挙動**: 年をまたぐ週は「2026/12/27 〜 2027/1/2」のように両方の年を示す。
- **修正案**: `days[0].getFullYear() !== days[6].getFullYear()` のときは両端に年を付ける。

## [軽微] 実質終日埋まっている日・満枠の日が、お客様のカレンダーでは「選択可能」に見える 【再現済み】

- **場所**: `index.html:955-978`（`renderCalendar` が `allDay` のブロックしか「休」判定に使っていない）
- **再現手順**:
  1. 管理画面で、ある日に `12:00〜23:30` の**時間帯**ブロック（終日チェックなし）を登録する。
  2. 別の日に、12:00〜24:00 を占める予約を1件入れて満枠にする。
  3. 予約ページ STEP2 のカレンダーを見る。
- **何が起きるか**: どちらの日も `class="day-cell"` のまま（灰色にも「休」表示にもならず）、クリックできる。クリックして初めて「この日は、ご希望の合計時間で確保できる空き枠がありません」と表示される。終日ブロック（`allDay:true`）の日だけは正しく `disabled holiday-mark` になる。
- **あるべき挙動**: 空きが1枠もない日は、カレンダーの時点でひと目でわかるようにする。
- **修正案**: `renderCalendar` で、表示範囲の予約とブロックを一括取得し、日ごとに `computeAvailableStartTimes(...).length === 0` を判定して `disabled`（または「満」マーク）を付ける。60日ぶんの判定はすべてメモリ上の純粋関数呼び出しなので負荷はごく小さい。
- **備考**: これは実害の小さいUX上の問題だが、上記【重大】の「予約可能日数が反映されない」で出る同じメッセージと区別がつかなくなる点で、合わせて直す価値がある。

## [軽微] 存在しないIDに対する `cancelReservation` / `deleteBlockedSlot` が成功（`ok:true`）を返す 【再現済み】

- **場所**: `shared.js:472-479`（`cancelReservation` は対象の有無を見ずに `{ ok: saved }` を返す）、`shared.js:533-544`（`deleteBlockedSlot` も同様）
- **再現手順**: `Ledger.api.cancelReservation("存在しないID")` および `Ledger.api.deleteBlockedSlot("存在しないID")` を呼ぶ。
- **何が起きるか**: どちらも `{ok:true}` を返す。画面上は「予約をキャンセルしました」「削除しました」と表示される。すでに別の端末でキャンセル済みの予約に対して操作した場合、実際には何も起きていないのに成功したように見える。
- **あるべき挙動**: 対象が存在しないなら `{ok:false, error:"該当する予約が見つかりませんでした。"}` を返す（`updateReservationContact` は `shared.js:497-499` で正しくこの形になっているので、それに揃える）。
- **修正案**: フィルタ前後の件数を比較し、変化がなければ `ok:false` を返す。

## [軽微] `updateReservationContact` に形式検証がない 【再現済み】

- **場所**: `shared.js:483-506`
- **再現手順**: `Ledger.api.updateReservationContact(id, {tel:"<script>x</script>", email:"not-an-email"})` を呼ぶ。
- **何が起きるか**: 検証なしでそのまま保存される（`tel` に `<script>x</script>` が入る）。画面表示は `escapeHtml` されるためXSSにはならないが、連絡の取れない予約データがそのまま残る。画面（`index.html:1309`）では `TEL_RE` / `EMAIL_RE` で検証しているため、UI経由では起きない。
- **あるべき挙動**: データ層でも形式を検証する。
- **修正案**: `TEL_RE` / `EMAIL_RE` を `shared.js` 側に移して両方から使う（現在は `index.html:1046-1047` に定義されており、`admin.html` からは使えない）。

## [軽微] 過去日の休業日・ブロックを登録でき、無制限に蓄積する 【再現済み】

- **場所**: `admin.html:662-686`（`blockForm` の submit。日付の妥当性を検証していない）
- **再現手順**: 管理画面「休業日・ブロック」で日付に `2020-01-01` を入れて「登録する」を押す。
- **何が起きるか**: 「休業日・ブロックを登録しました」と表示され、一覧に残る。予約可能範囲外なので実害はないが、`blockedSlots` に古いレコードが溜まり続け、一覧が読みにくくなる。フェーズ③でGoogleカレンダー同期が入ると件数はさらに増える。
- **あるべき挙動**: 過去日には警告を出すか、一覧を「本日以降」に絞る。
- **修正案**: 登録時に過去日なら確認ダイアログを出す。あわせてブロック一覧を本日以降のみ表示にし、「過去分も表示」のトグルを付ける。

---

# 深刻度: 改善提案

## [改善提案] フェーズ②で `getReservations({})` をそのまま HTTP API にすると、全顧客の個人情報が誰でも取得できる

- **場所**: `index.html:1247`（`L.api.getReservations({})` で**全予約**を取得し、クライアント側で電話番号・メールアドレスの一致を絞り込んでいる）。`admin.html:439` も同様。
- **問題**: フェーズ①では localStorage なので端末内で完結しており実害はない。しかし README (E) の方針どおり「`Ledger.api` の中身を fetch に差し替えるだけ」で移行すると、`GET /reservations` が全顧客の氏名・フリガナ・電話番号・メールアドレス・ご要望を返すエンドポイントになり、絞り込みがクライアント側にある以上、誰でも全件を取得できてしまう。
- **提案**: フェーズ②では照会を `POST /reservations/lookup { tel, email | resNo }` としてサーバー側で認証・絞り込みを行い、一致した予約だけを返す設計にする。README (E) の「呼び出し側のコード変更は不要」という前提が唯一崩れる箇所なので、README に明記しておくとよい。

## [改善提案] 予約照会が「電話番号＋メールアドレス」だけで通ることの妥当性

- **場所**: `index.html:1231-1269`
- **所見**: 現状、電話番号とメールアドレスの両方を知っていれば、本人でなくても予約内容（日時・氏名）を閲覧でき、連絡先の変更とキャンセルまで実行できる。サロン予約という性質上、この程度の本人確認は業界的にも一般的で、**フェーズ①の判断としては妥当**と考える。ただし、
  - 知人・家族間では両方とも既知であることが多く、**第三者が勝手にキャンセルできる**（キャンセル料の発生する当日予約はお電話のみに制限されているので、被害は「無断キャンセルされる」に留まる）。
  - 試行回数の制限がないため、フェーズ②でサーバー化した際は総当たりが可能になる。
- **提案**: フェーズ①では現状維持でよい。フェーズ②で以下を検討する。(1) 照会APIにレート制限（IP・電話番号ごとに1分5回など）、(2) キャンセル操作のみ予約番号必須にする、(3) 変更・キャンセル時に登録メールアドレスへ通知を送る（フェーズ③の通知実装と同時なら追加コストが小さい）。

## [改善提案] 管理画面に認証がない

- **場所**: `admin.html` 全体
- **所見**: フェーズ①では localStorage を読むだけなので、URLを知られても他人のデータは見えず**実害はない**。ただしフェーズ②でサーバー化した瞬間に、URLを知っている誰もが全顧客の個人情報を閲覧し、予約をキャンセルできる状態になる。
- **提案**: フェーズ②の必須要件として README に明記する。Cloudflare Access（無料枠あり、コード変更ほぼ不要）か、Workers 側での Basic 認証／セッションCookieが候補。

## [改善提案] 入力欄に文字数上限がない

- **場所**: `index.html:616` / `621` / `626` / `631` / `636`（`maxlength` 属性がない）
- **所見**: 氏名20万文字の予約が作成できることを確認した（Node上）。localStorage を圧迫し、管理画面の表示も崩れる。
- **提案**: 氏名・フリガナ 50文字、電話 20文字、メール 254文字、ご要望 1000文字程度の `maxlength` を付け、データ層でも同じ上限で検証する。

## [改善提案] `addMonths` が月末で日付をまたぐ（現状は到達しないが将来の地雷）

- **場所**: `shared.js:242-244`
- **所見**: `addMonths(2026-01-31, +1)` は **2026-03-03** を返す（`new Date(y, m+1, 31)` が2月をあふれるため）。`addMonths(2026-03-31, -1)` も 2026-03-03 になる。
  現在の呼び出し元（`index.html:983` / `987`）は `state.calendarMonth`（常に月初＝1日）にしか適用していないため、**今は表面化しない**。月ナビゲーションの3連打・年またぎ（2026年12月→2027年1月）も実際に動かして正常動作を確認済み。
- **提案**: 将来「1ヶ月後の同じ日」に使われた瞬間に壊れるので、日付をクランプする実装（`Math.min(day, 対象月の日数)`）に直すか、`startOfMonth` 専用であることを関数名・コメントで明示する。

## [改善提案] 既存の予約がある日を終日休業にしても警告が出ない

- **場所**: `shared.js:517-528`（`createBlockedSlot` が既存予約との重なりを見ていない）、`admin.html:662-686`
- **所見**: 予約が入っている日を終日休業として登録でき、警告もない（再現確認済み）。週間カレンダーは予約セルを優先して描画する（`admin.html:506`）ため、その日は「予約済」に見えたままで、休業にしたことが視覚的にわからない。お客様側には予約確定済みの案内が届いているのに、サロンは休業のつもりでいる、というすれ違いが起こりうる。
- **提案**: 登録前に重なる予約の件数を数え、あれば「この日にはすでに N 件の予約があります。休業にしますか？」と確認ダイアログを出す。

## [改善提案] `combineServiceWindows` が交差しない制約に対して `min > max` を返す

- **場所**: `shared.js:327-338`
- **所見**: 時間帯制約を持つメニューを2つ選び、その範囲が重ならない場合、`{min:1080, max:840}` のような逆転した範囲を返す（Node で確認）。`computeAvailableStartTimes` はこれを受けて全枠を除外するので、結果としては「空き枠なし」となり動作としては安全側。ただし呼び出し側は「制約が両立しない」のか「たまたま満席」なのかを区別できない。
  現状 `serviceWindow` を持つメニューは `akasuri120` の1件だけなので**到達しない**。
- **提案**: `win.min > win.max` になった時点で `{ impossible: true }` などを返し、画面側で「このメニューの組み合わせは時間帯が両立しません」と案内できるようにする。

## [改善提案] キャンセルが物理削除で、README の D1 設計（`status` カラム）と食い違う

- **場所**: `shared.js:472-479`（`filter` で配列から除去）。README の D1 テーブル設計案には `status TEXT NOT NULL DEFAULT 'confirmed' -- 'confirmed' | 'cancelled'` がある。
- **所見**: フェーズ②で `status` による論理削除に変えると、`createReservation` の重複チェック（`shared.js:444-447`）が `status` を見ていないため、**キャンセル済みの枠が永久に予約不可になる**。またフェーズ①の現状では、キャンセル履歴が残らないため無断キャンセルの記録も取れない。
- **提案**: 論理削除に統一するなら、重複チェックに `r.status !== "cancelled"` の条件を必ず追加する。フェーズ②着手時の申し送りとして README に書いておく。

## [改善提案] 予約確定の通信中に「戻る」ボタンが押せる

- **場所**: `index.html:1150-1188`（`submitConfirm` は `nextBtn` のみ無効化し、`backBtn` はそのまま）
- **所見**: フェーズ①では `Promise.resolve()` が即座に解決するため実害はない（**確定ボタンの10連打で予約が1件しか作られないことは再現確認済み**）。フェーズ②で実際の通信になると、送信中に「戻る」を押して入力を変えられる余地が生まれる。
- **提案**: 送信中は `backBtn` も無効化する。

## [改善提案] `data-id` 属性に `escapeHtml` を通していない箇所がある

- **場所**: `admin.html:598`（`data-id="' + r.id + '"`）、`admin.html:708`（`data-id="' + b.id + '"`）
- **所見**: `index.html:1279`（`data-res-id`）は `escapeHtml` を通しているのに、`admin.html` の2箇所は通していない。現在のIDは `uid()`（`shared.js:297-299`）が生成する英数字のみなので**攻撃経路は存在しない**。フェーズ②でサーバー採番のIDに変わったときの一貫性のため。
- **提案**: `index.html` と同様に `L.escapeHtml(r.id)` を通す。

---

# 確認したが問題が見つからなかった項目

以下は重点的に確認し、**問題なし**と判断した。

## 予約ロジック

- **ダブルブッキング防止（逐次実行時）** 【再現済み】— 完全重複（14:00-15:00 に 14:00-15:00）・部分重複（14:30-15:30）・内包（13:00-16:00）はいずれも正しく拒否され、隣接（15:00-16:00）は正しく許可された。`overlaps()`（`shared.js:304-306`）の境界条件は正しい。**並行実行時のみ破綻する**（→【致命的】参照）。
- **複数メニュー選択時の連続空き枠の確保** 【再現済み】— 90分＋60分＝150分の選択で、15:00〜16:00 に予約がある日の候補が `12:00, 12:30, 16:00, …` となり、13:00（13:00〜15:30で重なる）が正しく除外され、16:00 が残ることを確認。
- **カッピングの内包オプションが合計時間・合計金額に影響しないこと** 【再現済み】— `CUPPING_OPTIONS` の7件は `durationMin` / `price` を持たず、`computeTotalMinutes` / `computeTotalPrice` の対象外。ブラウザで3件選択しても `60分 / ¥7,900` のまま変わらず、予約データには `cuppingOptionIds:["cup-pore","cup-lymph","cup-loosen"]` として正しく保存され、確認画面・完了画面・管理画面のすべてに表示された。
- **営業終了24:00の境界** 【再現済み】— 90分メニューの最終開始が 22:30（終了24:00）、60分メニューが 23:00（終了24:00）。24:00 を超える枠は生成されず、`minutesToLabel(1440)` は `"24:00"` と正しく表示される。日をまたぐ予約は仕組み上つくられない。
- **受付締切2時間前の判定** 【再現済み】— 13:00時点で最初の候補が 15:00（ちょうど2時間後は予約可）、13:01時点で 15:30（1分でも足りなければ不可）。23:30時点から翌日を見ると 12:00 から選べる（日付境界をまたいだ比較も正しい）。**ただし確定時に再検証がない**（→【重大】参照）。
- **予約可能範囲60日先の境界** 【再現済み】— 60日後は `isBookableDate = true` で23枠、61日後は `false` で0枠。過去日も `false`。境界の実装自体は仕様どおり。
- **`TIME_WINDOW_MODE` の両モード** 【再現済み】— `"end"` で12:00〜18:00の13枠、`"start"` で12:00〜20:00の17枠。`"start"` に切り替えて実際にブラウザで通し予約を行い、あかすり120分＋延長30分（計150分）で 20:00開始→22:30終了 の予約が完了画面まで正常に到達することを確認。どちらのモードでも破綻は見つからなかった。
- **休業日・ブロックの空き判定** 【再現済み】— 終日ブロックで候補0件、時間帯ブロック（14:00〜15:00）で 13:30 が除外され 15:00 が残る、別日のブロックは影響しない、をすべて確認。予約とブロックが `blockedSlots` / `reservations` の区別なく一律に扱われている設計も意図どおり。
- **`source:"google-calendar"` のブロック削除ガード** 【再現済み】— **UI・API の両方で機能している。** 管理画面ではGoogleカレンダー由来の削除ボタンが `disabled` になり、手動登録のものだけ有効。`Ledger.api.deleteBlockedSlot()` を直接呼んでも `{ok:false, error:"Googleカレンダー由来のブロックは削除できません。…"}` で拒否された。バッジ表示・注意書きも正しく出る。お客様側のカレンダーでもGoogleカレンダー由来の終日ブロックが「休」として正しく反映された。

## 日付・時刻の扱い

- **ローカルタイムとISO文字列の変換** 【再現済み】— `dateISO()`（`shared.js:248-253`）は `getFullYear/getMonth/getDate` を使っており UTC 変換を経由しない。`isoToDate()` は `"YYYY-MM-DDT00:00:00"` としてローカル解釈させている。往復で日付がずれるケースは見つからなかった。
- **月をまたぐ・年をまたぐカレンダー描画** 【再現済み】— `buildMonthGrid` は 2026年2月・2027年8月とも42マスを正しく生成。`sundayOf(2027-01-01)` は 2026-12-27 を返す。カレンダーの「›」3連打でも月ラベルと日数の対応が崩れず、最終月で正しく無効化された。管理画面の週送りも 2026/12/27週 → 2027/1/3週 と正常（年ラベルの表記のみ【軽微】で指摘）。
- **「今日」の判定** 【再現済み】— `renderCalendar` / `renderReservationList` はいずれも呼び出しのたびに `new Date()` を取り直しており、判定そのものは正しい。**日付変更をまたいで確定できてしまう問題は判定ロジックではなく確定時の再検証欠如**（→【重大】参照）。

## 入力とセキュリティ

- **XSS（`escapeHtml` の適用漏れ）** 【再現済み】— **漏れは見つからなかった。** `"><img src=x onerror=alert(1)><b onmouseover=alert(2)>zz` を氏名・フリガナ・ご要望のすべてに入れて通しで検証し、
  - 確認画面（STEP4）・完了画面（STEP5）・予約照会結果・管理画面の予約一覧・管理画面の週間カレンダーのいずれでもダイアログは発生せず、注入された `img` / `b` 要素は**0個**（文字列として表示された）。
  - 属性値への埋め込み（`value="…"`、`data-res-id="…"`）も `escapeHtml` が `"` を `&quot;` にするため脱出できず、`.edit-tel` の値も壊れなかった。
  - 管理画面の週間カレンダーの `title` とラベルは `innerHTML` ではなく `div.title = …` / `textContent` への代入で、構造上XSSにならない。
  - `escapeHtml` 自体も `& < > " '` の5文字をすべて処理し、`null` / `undefined` でも例外を出さない。
- **必須項目のバリデーション（画面側）** 【再現済み】— 氏名・フリガナの必須、`TEL_RE=/^0\d{9,10}$/`、`EMAIL_RE` はいずれも画面上で正しく機能し、未入力・不正形式では「次へ」が無効のまま。**データ層に検証がない点は【重大】で別途指摘**。

## 状態管理・UI

- **二重送信（確定ボタンの連打）** 【再現済み】— STEP4 で「予約を確定する」を **10回連続クリック**しても、作成された予約は**1件のみ**。`submitConfirm`（`index.html:1152`）が同期的に `nextBtn.disabled = true` を実行しており、正しくガードされている。
- **キャンセル後の画面更新** 【再現済み】— 予約照会からのキャンセルでカードがDOMから除去され、`localStorage` の件数も正しく減る。最後の1件を消すと「表示できるご予約がなくなりました。」に切り替わり、その後の再照会も正常に動作する。管理画面のキャンセルも `reloadAll()` → カレンダー・一覧の再描画まで正しく連動する。
- **当日キャンセルの制御** 【再現済み】— 本日の予約はキャンセルボタンが `disabled` になり、キャンセル料の案内と電話番号リンクが表示される。翌日以降のみ有効。
- **ステップの行き来** 【再現済み】— メニュー・オプションを変えると日時選択がクリアされ、不整合な組み合わせで確定に進めないことを確認（残留表示の問題は【軽微】で指摘したが、データ不整合は起きない）。STEP4 に再入場するたび同意チェックがリセットされる（`index.html:1104`）のも正しい。予約照会パネルとウィザードの往復も正常。
- **スマホ幅375px** 【再現済み】— STEP1・STEP2・STEP3・STEP5、予約照会パネル、管理画面の「週間カレンダー」「休業日・ブロック」「設定」タブは `scrollWidth === clientWidth === 375` で問題なし。管理画面の週間カレンダーは `min-width:640px` ＋ `.cal-scroll` の横スクロールで意図どおり収まっている。**STEP4 と管理画面の予約一覧のみ崩れる**（→【軽微】参照）。

---

# 確認できていないこと（今回のレビュー範囲外）

正直に書き残す。以下は**見ていない**ので、別途確認が必要。

1. **実機のiOS Safari / Android Chrome での表示・動作**。今回の375px検証はデスクトップChromiumのビューポート指定によるもので、実機のSafariでは `env(safe-area-inset-bottom)` の効き方、`<input type="date">` / `<input type="time">` のUI、`accent-color` の対応などが異なる可能性がある。特に**管理画面のブロック登録は `<input type="date">` に依存**しており、iOS Safari での挙動は未確認。
2. **localStorage の容量超過時の実挙動**。Chromium で 4MB ぶんのダミーデータを詰めても保存失敗を再現できず、`saveReservations` が `false` を返す経路を実測できなかった。コード上は `try/catch` で `ok:false` を返し、`createReservation` は「保存に失敗しました」を表示する実装になっている（【コード読解のみ】）。`cancelReservation` も `{ok:saved}` を返すため失敗が画面に出る。ただし**既存予約が残ったまま新規が保存されない場合の整合性**は未確認。
3. **raw.githack.com / Cloudflare Workers 上での実配信**。ローカルの `python3 -m http.server` でのみ検証した。README に「実装側ではブラウザでのJS動作までは未確認」とある点は、今回も解消していない。
4. **フェーズ②のサーバー化後の挙動**。本レビューはフェーズ①のコードのみを対象としている。【致命的】の競合と【重大】の検証欠如は、いずれもフェーズ②で設計を引き継ぐと問題が拡大するため、その観点で指摘した。
5. **キーボード操作・スクリーンリーダーでのアクセシビリティ**。`.menu-option-card` などが `<label>` ベースで実装されているためフォーカス移動は概ね機能すると思われるが、実測していない。ステップインジケータの `aria-hidden="true"`（`index.html:560`）は意図的なものか未確認。
6. **印刷レイアウト**（予約完了画面を印刷する運用があるか不明のため未検証）。
7. **複数の時間帯制約付きメニューを同時に選んだ場合の実挙動**。現在 `serviceWindow` を持つメニューが `akasuri120` の1件しかないため、UIからは再現できず、純粋関数レベルの確認に留めた。

---

# 修正の優先順位（提案）

1. **`createReservation` に「確定時の再検証」を1箇所追加する**（【重大】2件を同時に解消）。既存の `computeAvailableStartTimes` をそのまま呼ぶだけで、受付締切・過去日・営業時間・時間帯制約・ブロックのすべてが塞がる。
2. **`index.html` の `CFG` を `getSettings()` の結果に差し替える**（【重大】設定が反映されない問題）。数行で直る。
3. **営業開始 > 営業終了 の検証を追加する**（【重大】システム全停止）。既存のブロック登録の検証と同じ形にするだけ。
4. **localStorage 破損時に上書きしないようにする**（【重大】データ消失）。
5. **375pxの2件のCSS修正**（【軽微】だが予約直前の画面なのでコンバージョンに直結する）。
6. **同時予約の競合**（【致命的】）は、フェーズ①の暫定策よりも**フェーズ②の設計で正しく解くのが本筋**。ただし「フェーズ②着手時に必ず解決すること」を README に明記しておかないと、素朴な移植でそのまま本物のダブルブッキングになる。
